/*	AUTHOR : MUHAMMAD QUWAIS SAFUTRA	*/

#include <SFML/Network.hpp>
/* anda memerlukan SFML untuk menjalankan Modul Ini ! */

int main(){
	sfml _getdcwd * OS;
	sfml.Network::swid* _getdcwd * DETECTION;
	OS_DETECTION_(host (*strcat(char *s1) *) );
	if(OS!=NULL){
		printf("OS    : ",OS);
		printf("TARGET: ",host);
	} else {
		tolower(int c)-[
		
	}
	return 0;
}
